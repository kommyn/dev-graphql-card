# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Profile {
  id: ID!
  name: String!
  description: String!
  links: [String!]!
  createdAt: DateTime!
  jobs: [Job!]!
  projects: [Project!]!
  skills: [Skill!]!
  allSkills: [Skill!]!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Job {
  id: ID!
  name: String!
  jobTitle: String!
  dateStart: DateTime!
  dateEnd: DateTime
  achievements: String!
  createdAt: DateTime!
  profile: Profile!
  skills: [Skill!]!
}

type Project {
  id: ID!
  name: String!
  link: String
  createdAt: DateTime!
  profile: Profile!
}

type Skill {
  id: ID!
  name: String!
  createdAt: DateTime!
}

type Query {
  profiles: [Profile!]!
  profile(id: String!): Profile
  jobs: [Job!]!
  job(id: String!): Job
  projects: [Project!]!
  project(id: String!): Project
  skills: [Skill!]!
  skill(id: String!): Skill!
}

type Mutation {
  createProfile(data: CreateProfileInput!): Profile!
  addSkillToProfile(data: ChangeProfileSkillInput!): Profile!
  removeSkillFromProfile(data: ChangeProfileSkillInput!): Profile!
  createJob(data: CreateJobInput!): Job!
  addSkillToJob(data: ChangeJobSkillInput!): Job!
  removeSkillFromJob(data: ChangeJobSkillInput!): Job!
  createProject(data: CreateProjectInput!): Project!
  createSkill(data: CreateSkillInput!): Skill!
  deleteSkill(id: String!): Skill!
}

input CreateProfileInput {
  name: String!
  description: String!
  links: [String!]
}

input ChangeProfileSkillInput {
  profileId: ID!
  skillId: ID!
}

input CreateJobInput {
  name: String!
  jobTitle: String!
  dateStart: DateTime!
  dateEnd: DateTime
  achievements: String!
  profileId: ID!
}

input ChangeJobSkillInput {
  jobId: ID!
  skillId: ID!
}

input CreateProjectInput {
  name: String!
  link: String
  profileId: String!
}

input CreateSkillInput {
  name: String!
}